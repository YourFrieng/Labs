4. Написати програму обчислення абсолютного значення введеного
числа.

#include <iostream>
using namespace std;


int main() {
	int a;
	cout << "Write num: ";
	cin >> a;
	a = (a < 0) ? a * -1 : a;


	cout << "Abs num: " << a;

	cin.get();
	return 0;
}
